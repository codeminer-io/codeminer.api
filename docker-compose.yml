services:
  codeminer-api:
    build: .
    ports:
      - "${CODEMINER_API_PORT:-8000}:8000"
    volumes:
      - ${CODEMINER_DB_PATH:?Set CODEMINER_DB_PATH}:/data/codeminer.duckdb:ro
    environment:
      - CODEMINER_DB_PATH=/data/codeminer.duckdb
    restart: unless-stopped
