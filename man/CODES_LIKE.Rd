% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{CODES_LIKE}
\alias{CODES_LIKE}
\title{Look up descriptions for clinical codes}
\usage{
CODES_LIKE(
  pattern,
  type = NULL,
  lookup_version = "latest",
  preferred_description_only = TRUE,
  col_filters = "default",
  .return_raw = FALSE,
  auth = auth_current()
)
}
\arguments{
\item{pattern}{a regular expression to search for}

\item{type}{character. Type of clinical code system to be searched. Optional
if input is a data frame with code_type column. Depends on what is
available in the lookup tables. See \code{\link[codeminer:add_lookup_table]{add_lookup_table()}} on how to add new
lookup tables. This can also be configured through the
\code{codeminer.code_type} option.}

\item{lookup_version}{character. Version of the lookup table to use. Default:
\code{"latest"}. Can be configured through the \code{codeminer.lookup_version}
option.}

\item{preferred_description_only}{logical. If \code{TRUE}, only returns the
preferred description for each code. Default: \code{FALSE}.}

\item{col_filters}{Column filters to apply. One of:
\itemize{
\item \code{"default"} (default): apply session-pinned or metadata-defined default
filters
\item \code{NULL}: no filtering (return all rows)
\item A named list of \code{column_name = c(values)} pairs for explicit filtering
}}

\item{.return_raw}{Logical. If \code{TRUE}, return raw httr2 response object.
If \code{FALSE} (default), parse JSON and return as tibble.}

\item{auth}{Authentication strategy (see \link{auth_strategies}). Defaults to
\code{\link[=auth_current]{auth_current()}}, which resolves to the session-wide setting from
\code{\link[=auth_use]{auth_use()}}, falling back to env-var-driven defaults via
\code{\link[=default_auth]{default_auth()}}. See \code{vignette("authenticated-requests")}.}
}
\value{
A \code{codeminer_codelist} object (tibble) containing the codes and their
descriptions
}
\description{
Returns a codelist with descriptions for the codes of interest. Supports
flexible input: character vectors, \code{||} separated strings, or data frames.
}
\details{
\code{CODES_LIKE} searches for codes that match a given regular
expression. The matching is case-insensitive.
}
\examples{
\dontrun{
CODES_LIKE(pattern = "^J45", type = "icd10")
}

}
\seealso{
\code{\link[codeminer:add_lookup_table]{add_lookup_table()}} for adding new lookup tables to the database.

Other Clinical code lookups and mappings: 
\code{\link[codeminer]{MAP}()},
\code{\link[codeminer]{get_lookup_table}()},
\code{\link[codeminer]{get_mapping_table}()},
\code{\link[codeminer]{get_relationship_table}()}
}
