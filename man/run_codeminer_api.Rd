% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_codeminer_api.R
\name{run_codeminer_api}
\alias{run_codeminer_api}
\title{Run the CodeMiner API}
\usage{
run_codeminer_api(
  host = "127.0.0.1",
  port = 8000,
  background = FALSE,
  docs = TRUE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{host}{Host address (default "127.0.0.1")}

\item{port}{Port number (default 8000)}

\item{background}{Logical. Run API in the background? (default FALSE)
When TRUE, returns a process handle that can be used to manage the server.}

\item{docs}{Logical. Should Swagger docs be enabled? (default TRUE)}

\item{quiet}{Logical. Suppress startup messages? (default FALSE)}

\item{...}{Additional arguments passed to \code{\link[plumber:pr_run]{plumber::pr_run()}}}
}
\value{
\itemize{
\item In foreground mode: Blocks until server is stopped (Ctrl+C)
\item In background mode: Returns invisibly a \link[callr:r_bg]{callr::r_bg} process object
}
}
\description{
Start the CodeMiner API server in either foreground or background mode.
The API exposes CodeMiner functionality as REST endpoints.
}
\section{Background Mode}{

When \code{background = TRUE}, the function returns a process handle with methods:
\itemize{
\item \verb{$is_alive()} - Check if the server is still running
\item \verb{$kill()} - Stop the server
\item \verb{$get_pid()} - Get the process ID
\item \verb{$read_output()} - Read server output
\item \verb{$read_error()} - Read server errors
}
}

\section{Environment Variables}{

The \code{CODEMINER_DB_PATH} environment variable must be set to the path
of your CodeMiner database. The value from the parent session is passed
explicitly to the background process. Project-level \code{.Renviron} files are
intentionally not loaded in the background process, so the value you set
via \code{Sys.setenv()} in your interactive session always takes precedence.
}

\examples{
\dontrun{
# Start in foreground (blocks until Ctrl+C)
run_codeminer_api()

# Start in background
bg <- run_codeminer_api(background = TRUE)

# Check if it's running
bg$is_alive()

# Stop the server
bg$kill()

# Custom host and port
run_codeminer_api(host = "0.0.0.0", port = 9000)
}

}
