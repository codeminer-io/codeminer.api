% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{get_codeminer_metadata}
\alias{get_codeminer_metadata}
\title{Get CodeMiner database metadata}
\usage{
get_codeminer_metadata(
  type = c("lookup", "mapping", "relationship"),
  .return_raw = FALSE,
  auth = auth_current()
)
}
\arguments{
\item{type}{Character vector of metadata types to return. Must be one or
more of \code{"lookup"}, \code{"mapping"}, \code{"relationship"}. Defaults to all three.}

\item{.return_raw}{Logical. If \code{TRUE}, return raw httr2 response object.
If \code{FALSE} (default), parse JSON and return as list of data frames.}

\item{auth}{Authentication strategy (see \link{auth_strategies}). Defaults to
\code{\link[=auth_current]{auth_current()}}; see \code{vignette("authenticated-requests")}.}
}
\value{
If a single type is requested, a data frame. If multiple types are
requested, a named list of data frames.
}
\description{
Returns metadata about the lookup, mapping, and relationship tables
available in the CodeMiner database connected to the API server.
}
\examples{
\dontrun{
# All metadata
get_codeminer_metadata()

# Just lookup tables
get_codeminer_metadata("lookup")

# Lookup and mapping
get_codeminer_metadata(c("lookup", "mapping"))
}

}
\seealso{
\code{\link[=check_api_connection]{check_api_connection()}} for diagnostics
}
