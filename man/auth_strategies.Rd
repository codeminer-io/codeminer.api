% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{auth_strategies}
\alias{auth_strategies}
\alias{auth_none}
\alias{auth_dev_user}
\alias{auth_custom}
\title{Authentication strategies}
\usage{
auth_none()

auth_dev_user(user_id = Sys.getenv("CODEMINER_DEV_USER", ""))

auth_custom(fn, description = "Custom auth strategy")
}
\arguments{
\item{user_id}{Character. User identifier sent in the \code{X-Dev-User} header.
Defaults to the value of the \code{CODEMINER_DEV_USER} environment variable.}

\item{fn}{Function. Takes an \code{httr2::request} and returns a (possibly
modified) \code{httr2::request}.}

\item{description}{Character. Human-readable description of the strategy,
shown by \code{print()} and \code{auth_describe()}.}
}
\value{
A function with class \code{codeminer_auth} that takes an \code{httr2::request}
and returns a (possibly modified) \code{httr2::request}.
}
\description{
A strategy is a function that receives an \code{httr2::request} and returns a
(possibly modified) request. It is applied immediately before the request
is performed, with URL and body already set, so strategies may modify any
aspect of the request.
}
\details{
\code{auth_none()} returns a strategy that leaves the request unchanged.

\code{auth_dev_user()} attaches an \code{X-Dev-User} header. Only useful when the
server is running with \code{CODEMINER_DEV_AUTH=1} (development setups).

\code{auth_custom()} wraps an arbitrary user-supplied request-modifier function
as a strategy with metadata. Use this to plug in auth schemes (bearer
tokens fetched from a separate auth service, signed requests, etc.) that
the package does not ship built-in. See
\code{vignette("authenticated-requests")} for worked examples.
}
\examples{
\dontrun{
# Attach X-Dev-User: alice to outgoing requests for this session
auth_use(auth_dev_user("alice"))
DESCRIPTION(pattern = "asthma")

# Custom strategy: bearer token from an env var
auth_use(auth_custom(
  function(req) httr2::req_auth_bearer_token(req, Sys.getenv("MY_TOKEN")),
  description = "Bearer token from MY_TOKEN env var"
))
}

}
\seealso{
\code{\link[=auth_use]{auth_use()}} to set a strategy session-wide; \code{\link[=auth_current]{auth_current()}} and
\code{\link[=auth_describe]{auth_describe()}} to introspect.
}
