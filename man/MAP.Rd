% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{MAP}
\alias{MAP}
\title{Map clinical codes from one coding system to another}
\usage{
MAP(
  ...,
  from = NULL,
  to = NULL,
  map_version = "latest",
  lookup_version = "latest",
  col_filters = "default",
  .return_raw = FALSE,
  auth = auth_current()
)
}
\arguments{
\item{...}{Codes to map. Supports flexible input like \code{\link[codeminer:CODES]{CODES()}}. Special
value: \code{"all"} returns all mapped codes.}

\item{from}{Coding system that \code{...} codes belong to. Optional if input is a
codelist with code_type.}

\item{to}{Coding system to map codes to.}

\item{map_version}{Version of the mapping table to use.}

\item{lookup_version}{character. Version of the lookup table to use. Default:
\code{"latest"}. Can be configured through the \code{codeminer.lookup_version}
option.}

\item{col_filters}{Column filters to apply to the \strong{mapping table}. One of:
\itemize{
\item \code{"default"} (default): apply session-pinned or metadata-defined default
filters
\item \code{NULL}: no filtering (return all rows)
\item A named list of \code{column_name = c(values)} pairs for explicit filtering
}

Note: this controls filtering of the mapping table, not the target lookup
table (which uses its own default col_filters).}

\item{.return_raw}{Logical. If \code{TRUE}, return raw httr2 response object.
If \code{FALSE} (default), parse JSON and return as tibble.}

\item{auth}{Authentication strategy (see \link{auth_strategies}). Defaults to
\code{\link[=auth_current]{auth_current()}}, which resolves to the session-wide setting from
\code{\link[=auth_use]{auth_use()}}, falling back to env-var-driven defaults via
\code{\link[=default_auth]{default_auth()}}. See \code{vignette("authenticated-requests")}.}
}
\value{
A \code{codeminer_codelist} of the mapped codes with their descriptions.

If using \code{codes = "all"}, returns the mapping table as a \code{data.frame} with
columns:
\itemize{
\item \code{from}: the codes from the source code system
\item \code{to}: the mapped codes from the destination system
}
}
\description{
Map clinical codes from one coding system to another
}
\details{
If no mapping table matching the \code{from -> to} direction is found,
but there is a table for \code{to -> from}, \code{MAP()} will return the reverse
mapping with a warning. Note that this is not guaranteed to be correct, as
most mapping tables only work one way.
}
\examples{
\dontrun{
MAP("J45", from = "icd10", to = "sct")
}

}
\seealso{
\code{\link[codeminer:add_mapping_table]{add_mapping_table()}} for adding new mapping tables to the codeminer
database.

Other Clinical code lookups and mappings: 
\code{\link[codeminer]{CODES}()},
\code{\link[codeminer]{get_lookup_table}()},
\code{\link[codeminer]{get_mapping_table}()},
\code{\link[codeminer]{get_relationship_table}()}
}
