% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{auth_use}
\alias{auth_use}
\alias{auth_clear}
\title{Configure session-wide authentication}
\usage{
auth_use(strategy)

auth_clear()
}
\arguments{
\item{strategy}{A strategy created by \code{\link[=auth_none]{auth_none()}}, \code{\link[=auth_dev_user]{auth_dev_user()}},
\code{\link[=auth_custom]{auth_custom()}}, or any function with the same shape.}
}
\value{
\code{auth_use()} returns the strategy invisibly. \code{auth_clear()} returns
\code{NULL} invisibly.
}
\description{
\code{auth_use()} sets the auth strategy used by all \code{codeminer.api} helpers
for the rest of the R session. \code{auth_clear()} removes any session-wide
setting, falling back to \code{default_auth()}.
}
\details{
Per-call overrides via the \code{auth} parameter on each helper still take
precedence over the session-wide setting.
}
\examples{
\dontrun{
auth_use(auth_dev_user("alice"))
DESCRIPTION(pattern = "asthma")  # uses alice for the rest of the session

auth_clear()  # back to default
}

}
