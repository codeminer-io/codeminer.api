% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{CHILDREN}
\alias{CHILDREN}
\title{Retrieve parent or child codes}
\usage{
CHILDREN(
  ...,
  type = NULL,
  lookup_version = "latest",
  relationship_version = "latest",
  preferred_description_only = TRUE,
  col_filters = "default",
  .return_raw = FALSE,
  auth = auth_current()
)
}
\arguments{
\item{...}{Codes to start from. Supports flexible input like \code{\link[codeminer:CODES]{CODES()}}.}

\item{type}{Code type (character).}

\item{lookup_version}{Lookup table version (character).}

\item{relationship_version}{Relationship table version (character).}

\item{preferred_description_only}{Logical. If \code{TRUE}, return only preferred
descriptions.}

\item{col_filters}{Column filters to apply. See \code{\link[codeminer:CODES]{CODES()}} for details.}

\item{.return_raw}{Logical. If \code{TRUE}, return raw httr2 response object.
If \code{FALSE} (default), parse JSON and return as tibble.}

\item{auth}{Authentication strategy (see \link{auth_strategies}). Defaults to
\code{\link[=auth_current]{auth_current()}}, which resolves to the session-wide setting from
\code{\link[=auth_use]{auth_use()}}, falling back to env-var-driven defaults via
\code{\link[=default_auth]{default_auth()}}. See \code{vignette("authenticated-requests")}.}
}
\value{
A data frame of codes and descriptions.
}
\description{
Returns immediate or transitive parent or child codes for the given codes by
traversing the relationship graph.
}
\details{
Use \code{N_PARENTS()}/\code{N_CHILDREN()} for immediate relationships (one step), and
\code{PARENTS()}/\code{CHILDREN()} for transitive closure (all reachable
ancestors/descendants).
}
\examples{
\dontrun{
CHILDREN("73211009", type = "sct")
}

}
\seealso{
Other Code relationships: 
\code{\link[codeminer]{attributes}()},
\code{\link[codeminer]{relationship_types}}
}
