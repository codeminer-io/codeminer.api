% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{get_relationship_tree}
\alias{get_relationship_tree}
\title{Build a flat nodes/edges tree view for a set of codes}
\usage{
get_relationship_tree(
  codes,
  type = NULL,
  expand_to_descendants = TRUE,
  max_codes = 10000,
  relationship_version = "latest",
  lookup_version = "latest",
  col_filters = "default",
  preferred_description_only = TRUE,
  .return_raw = FALSE,
  auth = auth_current()
)
}
\arguments{
\item{codes}{Character vector of seed codes.}

\item{type}{The code type (character). Can also be configured via the
\code{codeminer.code_type} option.}

\item{expand_to_descendants}{Logical. If \code{TRUE} (default), \code{codes} are
expanded to include all descendants via \code{\link[codeminer:graph_closure]{graph_closure()}} before
collecting edges and nodes. If \code{FALSE}, \code{codes} is used as-is.}

\item{max_codes}{Integer. Maximum size of the (expanded) code set.
Aborts with class \code{codeminer_max_tree_codes_exceeded} if exceeded.
Defaults to \code{getOption("codeminer.max_tree_codes", default = 10000)} —
a guardrail against accidentally materialising a SNOMED-sized
subgraph in R memory.}

\item{relationship_version}{Relationship table version. Defaults to
\code{"latest"}. Can be configured via the \code{codeminer.relationship_version}
option.}

\item{lookup_version}{Lookup table version. Defaults to \code{"latest"}. Can
be configured via the \code{codeminer.lookup_version} option.}

\item{col_filters}{Column filters to apply to both the relationship
and lookup tables. See \code{\link[codeminer:CODES]{CODES()}} for details.}

\item{preferred_description_only}{Logical. If \code{TRUE} (default), node
\code{term} is the preferred description only (one row per code).}

\item{.return_raw}{Logical. If \code{TRUE}, return raw httr2 response object.
If \code{FALSE} (default), parse JSON and return as tibble.}

\item{auth}{Authentication strategy (see \link{auth_strategies}). Defaults to
\code{\link[=auth_current]{auth_current()}}, which resolves to the session-wide setting from
\code{\link[=auth_use]{auth_use()}}, falling back to env-var-driven defaults via
\code{\link[=default_auth]{default_auth()}}. See \code{vignette("authenticated-requests")}.}
}
\value{
A plain \code{list} with two tibbles:
\describe{
\item{\code{nodes}}{\code{code}, \code{term}, \code{category}, \code{in_input_set} (logical:
\code{TRUE} for codes in the original input, \code{FALSE} for codes added by
descendant expansion).}
\item{\code{edges}}{\code{parent}, \code{child} — one row per hierarchical edge among
the (expanded) code set.}
}

Orphan codes (in the set but with no hierarchical edges) appear in
\code{nodes} with no rows in \code{edges}.
}
\description{
Given a set of seed \code{codes}, returns a \code{list(nodes, edges)} suitable for
hierarchy analysis or tree rendering (e.g. \code{data.tree}, \code{ggraph},
\code{visNetwork}). By default the seed is first expanded into its full
descendant set, then the parent/child edges \emph{among} that set are returned
alongside a node table carrying term and category.
}
\details{
Internally composes \code{\link[codeminer:graph_closure]{graph_closure()}} (for descendant expansion) and the
filtered \code{\link[codeminer:get_relationship_table]{get_relationship_table()}} / \code{\link[codeminer:get_lookup_table]{get_lookup_table()}} getters. Edges
are restricted to the hierarchical relationship type defined by
\code{child_parent_relationship_code} in the relationship metadata — non
hierarchical edges (e.g. SNOMED "has finding site") are excluded.
}
\section{Options}{


\itemize{
\item \code{codeminer.max_tree_codes}: default size cap for the expanded code set.
Overridden by the \code{max_codes} argument.
\item \code{codeminer.code_type}, \code{codeminer.lookup_version},
\code{codeminer.relationship_version}: shared with \code{\link[codeminer:CODES]{CODES()}} / \code{\link[codeminer:CHILDREN]{CHILDREN()}} /
\code{\link[codeminer:PARENTS]{PARENTS()}}, used as defaults for the corresponding arguments.
}

}

\examples{
\dontrun{
get_relationship_tree("E10", type = "icd10")
}

}
\seealso{
\code{\link[codeminer:get_relationship_table]{get_relationship_table()}} for raw edges,
\code{\link[codeminer:get_lookup_table]{get_lookup_table()}} for terms, \code{\link[codeminer:PARENTS]{PARENTS()}} / \code{\link[codeminer:CHILDREN]{CHILDREN()}} for
flat codelist traversal.

Other Clinical code lookups and mappings: 
\code{\link[codeminer]{CODES}()},
\code{\link[codeminer]{MAP}()},
\code{\link[codeminer]{get_lookup_table}()},
\code{\link[codeminer]{get_mapping_table}()},
\code{\link[codeminer]{get_relationship_table}()}
}
