test_endpoint_addition(
  add_description_endpoint,
  "add_description_endpoint",
  "/DESCRIPTION"
)

test_endpoint_addition(add_codes_endpoint, "add_codes_endpoint", "/CODES")

test_endpoint_addition(
  add_codes_like_endpoint,
  "add_codes_like_endpoint",
  "/CODES_LIKE"
)

test_endpoint_addition(
  add_children_endpoint,
  "add_children_endpoint",
  "/CHILDREN"
)

test_endpoint_addition(
  add_n_children_endpoint,
  "add_n_children_endpoint",
  "/N_CHILDREN"
)

test_endpoint_addition(add_parents_endpoint, "add_parents_endpoint", "/PARENTS")

test_endpoint_addition(
  add_n_parents_endpoint,
  "add_n_parents_endpoint",
  "/N_PARENTS"
)

test_endpoint_addition(
  add_attributes_for_endpoint,
  "add_attributes_for_endpoint",
  "/ATTRIBUTES_FOR"
)

test_endpoint_addition(
  add_has_attributes_endpoint,
  "add_has_attributes_endpoint",
  "/HAS_ATTRIBUTES"
)

test_endpoint_addition(
  add_relationship_types_from_endpoint,
  "add_relationship_types_from_endpoint",
  "/RELATIONSHIP_TYPES_FROM"
)

test_endpoint_addition(
  add_relationship_types_to_endpoint,
  "add_relationship_types_to_endpoint",
  "/RELATIONSHIP_TYPES_TO"
)

test_endpoint_addition(add_map_endpoint, "add_map_endpoint", "/MAP")

test_endpoint_addition(
  add_metadata_endpoint,
  "add_metadata_endpoint",
  "/metadata"
)
