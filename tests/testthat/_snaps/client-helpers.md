# client replays warnings and messages via CLI

    Code
      print_captured_warnings_and_messages(list(messages = messages, warnings = warnings))
    Condition
      Warning:
      ! two
    Message
      i one

